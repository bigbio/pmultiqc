name: pmultiqc
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - multiqc==1.19.0
  - pandas
  - pyteomics
  - pyopenms
  - sdrf-pipelines >= 0.0.28
  - lxml
  - numpy
  - pyarrow

